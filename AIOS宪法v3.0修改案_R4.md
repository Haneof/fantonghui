# AIOS 核心系统宪法 v3.0 修改案 R4

**状态**：待批准（Architect 签核前不生效；签核后成为 v3.0 基线的组成部分）
**日期**：2026-09-16
**提案人**：独立首席系统架构师（基于《AIOS宪法v3.0_首席评审报告》2026-09-15 与《AIOS_Core_工程重构与任务拆分设计书_R4》）
**修改分级**（第 115 条）：R4-01/02/03/05 为**架构修改**（触及核心概念与契约清单）；R4-04/06/07/08 为**机制优化**；R4-09 为**实现优化**（新增编号的验收行本身属架构修改级）。
**配套工程**：M0′ 契约增量冻结段 M0-023~030；CAM 验收矩阵 `schemas/constitution_acceptance.py`；设计书 §3.4 五个任务的 A–J 详规。

> 每一项按第 115 条格式给出：修改什么 / 为什么 / 现有机制哪里失败 / 测试证明 / 影响模块 / 旧数据兼容。

---

## R4-01【架构修改】新增一等对象 Reinterpretation，裁决"回溯标注"唯一语义

**修改什么**
1. 第三编第二十一章第 71 条对象清单追加第 23 项：

   | 23 | Reinterpretation（回溯解释层） | AI 基于新获知内心数据/后续证据，对历史节点**追加**的标注对象；append-only，永不改写被指向对象 |

2. 对象核心字段（冻结进 M0-027）：
```text
reinterpretation_id            # 复用全局稳定 ID 机制
target_ref                     # 必须 pinned 到精确 revision（第 18 条指针主义）
slot                           # 注册制枚举：EMOTION | MEANING | IDENTITY_TAG（禁止自由槽名）
statement / confidence / evidence_set_id / supersedes / valid_time_range
公共字段                        # occurred_at = 标注诞生时间（T_now），不是被标注区间
```
3. 第 31 条之一第 1 款"反向反哺加注权限"改写为：
   > AI 获知内心事实后，通过**创建 Reinterpretation 对象**为历史时空切片追加解释层；被加注节点本身永不改动。历史查询区分两种透镜：`AS_KNOWN`（当时 AI 看到的世界，评估判分与回放默认）与 `ANNOTATED`（叠加当前有效标注层的完整世界，叙事与呈现用）。
4. 第 81 条末句"修正原有的平稳曲线"改为"**为原有的平稳曲线追加可展开的标注层**"。
5. 第 110 条第 13 项契约扩写为："认知修正通过依赖关系传播，不直接覆盖历史；历史节点的事后解释以 Reinterpretation 追加对象承载，双透镜读取语义为冻结契约。"

**为什么**：现行第 31 条之一（"倒带回去给时空切片追加标注"）与第 93 条（"新认知只写在 T_now，历史不可改写"）互斥，且第 71 条 22 个对象无一能承载"回溯标注"——写进 Observation 违反"原始输入不含主观判断"，写进 Claim 污染 `occurred_at` 语义。

**现有机制哪里失败**：M0-020 `world_at(T)` 与 M0-019 pinned 引用建立在"节点内容+知识截止"两元假设上；第 81 条把回溯标注写成触发章节的"必须"，一旦 M3 实现传播（M3-001），时态语义分裂将迫使 M0-005/M0-020 全部重审——M0 已发生的 R4/B6/B7 引用可见性三轮返工是同类歧义的实证。

**测试证明（新增 R4-06 验收）**：同一 T 双视图确定性分叉；标注后原节点 revision 计数不变（哈希审计）；万条标注压力测试传播工作量 = O(引用该标注的对象数)，有预算上限；"心里憋屈表面平稳"场景（V36）中，评估器对"当时应知"的判分仅使用 AS_KNOWN 透镜。

**影响模块**：C02（双透镜读面）、C06（裁决器，责任模块）、C07（传播边界：标注撤销只波及引用它的上层总结）、C14（时态真值集）。
**旧数据兼容**：无存量 Reinterpretation；历史回放默认 `view=AS_KNOWN`，行为与现行完全一致；`ANNOTATED` 为纯增量只读能力。

---

## R4-02【架构修改】写入来源分类 `source_class` 与触发豁免（新增"第 79 条之二"）

**修改什么**：在第 79 条后新增：
> **第 79 条之二：写入来源分类与触发豁免**
> 1. 每一次世界写入必须声明来源分类：`USER / SENSOR / AI_COGNITION / MAINTENANCE / SAFETY`。OperationRequest 与 world commit 携带该字段，作为冻结契约。
> 2. `MAINTENANCE`（STALE 标记、总结重建、归档提案、索引元数据、策略同步）对第 79 条机械触发评估**不可见**：维护写不得直接产生 Wake；后续认知处理一律经由第 63 条复核任务通道进入。维护写仍保留完整版本记录与审计（第 86.4 不变）。
> 3. `SAFETY` 由固件桥与内核调度器专属持有，AI 会话无权限声明；`ai_worker` 默认且仅可 `AI_COGNITION`。
> 4. 豁免判定只读 `source_class` 字段，**禁止以语义识别"这像不像维护内容"**（第 77/106 条不得被本条绕过）。

同时第 79 条第 9 项"与任务依赖相关的新资料到达"括注限定："不含 MAINTENANCE 类写入"。

**为什么**：现行 64 条只禁"递归创建 Wake"、82 条只压"同源重复命中"，均拦不住"维护写被当作新资料 → 触发 → 唤醒 → 再维护写"的认知永动机；第 86.4 反而强制维护写走完整版本记录，使其必然产生触发可见性。
**现有机制哪里失败**：M2-002 实现方式（"Observation commit 后把变更摘要投递 trigger engine"）订阅 commit 流不区分写入者；与 M3-001 传播引擎在 M4 三十天加速运行下形成唤醒风暴，虚拟时钟（M2-001 `advance_to_next`）因队列永不空而无法推进——"不报错，只是不动"。
**测试证明（R4-05 验收）**：V21b 三十天风暴回归：维护写 1 万笔 → 0 Wake；唤醒放大比 ≤1；虚拟时钟推进吞吐下限；ai_worker 声明 MAINTENANCE/SAFETY → PERMISSION_DENIED。
**影响模块**：C02（字段与迁移）、C06/C07（MAINTENANCE 生产者）、C09（豁免表）、C13（预算记录以 world_revision 关联，不入世界库以免自激）。
**旧数据兼容**：迁移脚本将 M0 存量 `world_commits` 显式回填为 `AI_COGNITION`（禁止 DEFAULT 蒙混：回填 SQL 进 M0-022R 复审证据链）；旧库无该字段的读取路径一律报错而非默认放行。

---

## R4-03【架构修改】第 33 条第 5 款"物理删除"改为"归档提案权"

**修改什么**：第 33 条第 5 款末段替换为：
> 大模型每日复盘拥有的是**归档提案权而非物理删除权**：判定为噪声的碎片标记 `PRUNED`（tombstone 修订，保留对象与可重建引用，source_class=MAINTENANCE），移入冷存层；仅当"零引用 ∧ 超龄 ∧ 用户显式授权"三条件合取时，方可离线压缩。已确立事件、关键原话、核心证据链永存——永存的实现方式是 append-only。

第 15 条第 14 项违宪清单同步改写："禁止绕过三条件合取的物理删除"（原"由大模型……智能清洗剔除"措辞废止）。
**为什么**：物理删除权交给每日运行、有幻觉概率、且与被评估对象同源的 LLM，是不可逆操作+不可审计（删除连 tombstone 都没有则"删了什么"无从考证）；且与第 25/93/116 条直接冲突——**宪法允许另一条宪法违宪**。
**现有机制哪里失败**：M0 存储（append-only `object_revisions`）今天没有任何删除路径——33.5 在现行契约下不可实现，属于死条款或 M1 私开后门的双选一。
**测试证明（R4-07 验收）**：全库删除审计：任何非三条件合取的物理删除 = 红灯；PRUNED 对象对其引用者仍可解析（返回 tombstone+重建提示）；V35 影子双世界：清洗组效用 ≥ 全留组 − ε 且存储显著下降，否则本机制在 M8 被消融出局。
**影响模块**：C01（提案产生点）、C02（tombstone 修订 + 冷存层）、C14（V35 对照）。
**旧数据兼容**：无删除先例，纯增量语义。

---

## R4-04【机制优化】第 87 条更名与定义；第 89 条延迟单位统一

**修改什么**
1. 第 87 条标题"5D 时间镜头滑动条"改为"**时间镜头（Time Lens）与多尺度自由导航**"；正文补充定义：镜头 = 缩放（zoom）/平移（shift）/框选（select）三操作 × 维度开闭与并行对比（第 88.1/88.2 条）——**全文从未定义过"5D"是哪五维，停止使用未定义概念**。
2. 增加层级诚实条款：`1s/10s 层仅含稀疏事件（显著运动点、转写句、异常标记），与第 33.1 条"严禁高频灌水"共同生效；镜头指向无数据层级时返回 `EMPTY_LAYER`，禁止以推断填充。
3. 第 89 条统一为："共现检索 **p95 ≤ 50ms @ 50 万对象修订**（虚拟时钟域，CI 基准机）"，删除"秒级"与"毫秒级"并存的表述。
**为什么**：87 条承诺"微秒/1s 微观下钻"与 33 条禁存高频正面相撞——宪法禁止存的东西被另一条宪法承诺随时可钻；89 条同一条内"毫秒级/秒级"自相矛盾，验收无从下笔。
**现有机制哪里失败**：M1-010/011 时间镜头任务无法对空层级定义行为；M7-002 首次压测前无人对"毫秒"负责。
**测试证明**：R4-02（G-M1P 规模闸）+ `EMPTY_LAYER` 行为单测。
**影响模块**：C11（物理实现）、C05。
**旧数据兼容**：无——纯语义澄清。

---

## R4-05【架构修改】第 108 条 C 表补录 C11；第 109 条 M0 冻结清单补全

**修改什么**
1. 第 108 条模块边界表新增一行：
   > **C11 世界检索与索引** | FTS 关键词/别名索引、共现交集、时间镜头物理面、索引水位与重建、热卡（HotCard）预计算 | 索引是世界的**可重建投影**：读响应必须返回 `(world_revision, index_watermark, lag)`；禁止索引内容与 payload 形成语义拷贝；禁止检索结果绕过知识截止可见性。

   同条"14 组件"清单第 5 号"世界搜索引擎"自此有模块承接。旧规划 C06"世界查询"编号作废，以本表为唯一账本。
2. 第 109 条 M0"必须冻结项"追加：`Summary 结构（第 59 条）、LifeChapter 结构（第 29 条）、Prediction Register（第 50–53 条）、OperationExperience/CommunicationExperience/ToolProposal（第 67–70 条）、Reinterpretation（R4-01）、source_class（R4-02）、BudgetPolicy/MeteringRecord/AssemblyPolicy（R4-08）、runtime_profile（R4-09）`。
3. M0 任务表追加 M0-023~M0-030（契约增量冻结段），M0-022 复审改签 **M0-022R**；已 FINAL PASS 的 M0-001~021 不重开。
**为什么**：第 71 条 22 个对象中 7 个不在任何冻结清单，却在 M3/M5 被依赖、被 V3-03/R3 验收引用——M1 开工即踩空；组件 5"世界搜索引擎"在 C 边界表无住房，是检索被排成二等公民的制度根源。
**现有机制哪里失败**：M1-012/014 以"任务书一句话"承载宪法三条性能承诺；`TASK_PROGRESS_R2.md` 当前基线行仍写"AIOS宪法2.0.txt + R1/R2"——工程与宪法版本脱钩的实证。
**测试证明**：CAM 矩阵 CI 门（`tests/architecture/test_cam_coverage.py`）：第 114 条全部 46 项 + R4 新增 9 项 = 55 项 100% 映射，任何未映射验收项红灯。
**影响模块**：治理工件（108/109/114 条文本）、C11 新设。
**旧数据兼容**：M0 存量契约与测试原样保留；新增冻结项以新编号新 Gate 进入。

---

## R4-06【机制优化】第 82 条安全通道例外与去抖

**修改什么**：第 82 条"安全信号不得被普通冷却规则屏蔽"后补：
> 但安全通道必须自带**去抖带与确认窗**：阈值边缘抖动合并为单次确认；确认通过前不得向用户输出紧急震动。防误触优先于防漏报的呈现节奏，漏报由固件确认窗兜底（第 78.4 条固件侧负责迟滞参数，AI 不可下调，与第 83.2 条同构）。

**为什么**：现行条文禁止对安全信号做常规冷却，摔倒检测抖动场景下等于宪法**明文授权**"手腕上的秃鹫"（连续紧急强震）。
**测试证明**：V32：50 次/秒阈值抖动 → 去抖后 ≤1 条强震指令且过确认窗；安全唤醒不被普通抑制器吞掉。
**影响模块**：C09、C15。旧数据兼容：无。

---

## R4-07【机制优化】第 80 条心跳机械预过滤（零 Token 准入）

**修改什么**：第 80.2 条补款：
> "此刻是否方便"的判定属第 106 条"格式整理/信号处理"权宜范围：由机械日历状态机（工作时段/深夜/驾驶/专注区间 + 上次震动响应率统计）先行裁决，**预过滤不通过则不产生模型调用**；被拒绝的心跳记 `HEARTBEAT_SUPPRESSED` 供回放，不得假装醒过。预过滤通过后，唤醒仍须携带 C13 预算 grant。

**为什么**：80.1"每 3~5 小时"+80.3"后台巡检永不停转"，叠加 46/106 条禁止本地做最终判断——现行条文组合迫使每天 5–8 次纯 LLM 空转唤醒去决定"什么都不做"，与 53/86 条零浪费正面冲突。
**测试证明**：R4-03（调度器与预过滤路径 LLM 调用数 = 0，除非 grant 通过且进入会话）。
**影响模块**：C08/C09/C13。旧数据兼容：无。

---

## R4-08【架构修改】新增预算契约与唯一模型出口；第 114 条增设 R4 验收行

**修改什么**
1. 新增契约（并入 R4-05 冻结清单）：
   > **第 86 条之一：经济控制**。世界模型新增 `BudgetPolicy`（scope：轮/日/后台日/维护任务/穿戴摄入；on_exceed：checkpoint/degrade_rules/defer_to_idle/hard_deny）与 `MeteringRecord`（记 operations 账本，**不入世界库**，防计量写自激）。模型调用唯一出口为接入层网关：无 grant 的调用 = 协议违例。流式萃取上限 1 次/8 轮 ∧ ≤40 次/日（溢出**有界延迟合并**入队，"绝不积压"条款据此和解为"有界积压"）；预算对象可由经验经 ToolProposal 修订，安全与摄入底线不可修订。
2. 第 114 条新增验收表（R4 系列，供 CAM 矩阵直接映射）：

| 编号 | 验收项 | 判定 |
|---|---|---|
| R4-01 | 首字延迟预算 | V34 链路（含 ASR 尾点与网络固定 RTT 注入）p95 ≤ 1.0s；深召回迟到走"句尾补注"，不得延迟首句 |
| R4-02 | 检索规模闸 | G-M1P：50 万修订共现检索 p95 ≤ 50ms；`EXPLAIN QUERY PLAN` 无 payload 全扫；G-M1P/M4/M7 三点位一致（±20%） |
| R4-03 | 调度零浪费 | 1 万 armed 谓词 × 30 天：评估数 = O(命中)，路径 LLM 调用 = 0；崩溃重放幂等；时钟推进吞吐达标 |
| R4-04 | 心智四步序可验证 | 91 条原子操作埋点 + 回放断言 `mirror→rapport→posture→world`；乱序/跳步计违宪 |
| R4-05 | 风暴回归 | V21b：唤醒放大比 ≤1；维护写 0 Wake；ai_worker 越权声明 MAINTENANCE/SAFETY 必败 |
| R4-06 | 双视图裁决 | 同一 T 双透镜确定性分叉；标注不增改目标 revision；传播爆炸半径 = O(引用数) |
| R4-07 | 归档审计 | 全库无非三条件合取的物理删除；PRUNED 可解析可重建 |
| R4-08 | 影子对照 | V35：清洗组效用 ≥ 全留组 − ε 且存储显著下降，否则 33.5 机制 M8 出局 |
| R4-09 | 注入防线 | V31：第三方 REPORTED 不升为高置信 FACT、不触发对外行动；source_trust 账本完整 |

**为什么**：85"1 秒"、86"零浪费"、53"防 Token 滥用"三处承诺在验收表无一行数字；M4-002 的成本是事后统计而非运行时闸门——两套账本必然一真一假。
**测试证明**：即本表本身（R4-01~09），由 M2-018/M4-005/G-M1P 交付。
**影响模块**：C13（升格经济控制）、C10、C14、M0′ 契约。
**旧数据兼容**：MeteringRecord 新表对 M0 存量无迁移需求。

---

## R4-09【机制优化+实现优化】Claim 信任字段；多源"平等互证"限定；band_v0 profile 契约

**修改什么**
1. 第 38 条 Claim 字段表追加：
   > `source_trust`（来源主体可信度，0–1，随反馈演化）；`corroboration_required`（bool：第三方转述类内容置真时，未获用户侧或独立源印证前，**不得驱动主动帮助行为与外部副作用任务**，仅可入候选认知）。
2. 第 19.1 条"各源平等互证"补限定："平等指**记录权利**平等，不指**行动授权**平等；行动授权按 R4-09.1 的信任字段分层。"
3. 新增 runtime_profile 契约（M0-030）：`virtual / band_v0` 双配置决定 C01 摄入硬约束、C13 延迟预算、C02 存储速率上限；**band_v0 冒烟自 M2 起为每里程碑出口强制项**。
**为什么**：33.4/19.1 让 IM 与旁听语音"平等互证"却无信任模型——同事玩笑可一路合宪地长成提醒任务；穿戴端约束（电池/带宽/算力）在宪法里是许诺，无 profile 则 M1–M7 会按虚拟假设过度优化。
**现有机制哪里失败**：M4/M7 用干净模拟数据评测，对抗噪声为零——M8 消融将系统性偏乐观。
**测试证明**：R4-09 注入场景 + band_v0 一日回放红灯机制。
**影响模块**：C06、C01、C13、C14。
**旧数据兼容**：存量 Claim `source_trust` 回填 0.5（中性）、`corroboration_required` 按 claimant≠subject 规则推导。

---

## 生效方式

本修改案经 architect-01 复审、chief-01 签署后，与 M0-023~030 契约冻结同批生效；第 114 条 R4 验收行自生效日起进入 CAM 矩阵强制映射（`tests/architecture/test_cam_coverage.py` 已在仓库，当前将 R4 项标记为 `proposed_pending_R4`，批准即转 `planned_M2/M3/M4`）。本修改案不触碰 M0-001~021 已 FINAL PASS 的任何契约文本。
