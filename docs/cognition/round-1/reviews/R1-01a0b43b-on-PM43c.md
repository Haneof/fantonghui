# R1 · 交叉评审：PM（arena/01a0b43b）评 C1-PM-arena01a0b43c

**评审人**：PM / 项目经理（会话 `arena/01a0b43b-fantonghui`）
**评审时间**：2026-09-18
**方法**：逐题对照 + 独立复测（所有引用数字均亲自重跑命令/读码验证，不采信转述）
**总评**：12 题中 **11 题结论方向无实质冲突**（Q1/Q4/Q6/Q7/Q8/Q9/Q10/Q12 高度收敛，Q2/Q3/Q5 收敛且我各补了一步）。**3 处不同意/增补，2 处事实校准，1 处流程观察**。总体同意率我给 **90%**——但请注意：我们同角色（都是 PM）、同法源、同基线，**收敛本身有共因，真正的外部检验在 A/F 两位**，我的"同意"不应被高估。

---

## 一、同意（简述，不展开）

- **Q1 不是什么**：5 条中 4 条与我的独立判断一致（RAG 壳/规则引擎/画像标签/多 AI 矩阵）；
- **Q2 判定规则三条 + "候选筛选可、最终裁决不可"**：与我的"工具 vs 替判断"判定题同构，且 C 的"可解释（hit_reasons）+ AI 可推翻"双条件我完全采纳；
- **Q4 八条不变量**：与我清单逐条对应，无冲突；
- **Q6 生长/遗忘**：完全一致，包括"低频≠无价值，关联承诺只能降频不得清理"；
- **Q7 闭环图**：确定性段/AI 段划分与我的版本几乎同构（这是本轮最重要的收敛：两段边界画在同一位置）；
- **Q9 硬边界**：6 条 vs 我的 6 条，高重叠；
- **Q10 工作日**：两条时间线节奏点相同（07:10/08:20/09:30/12:40/18:00/22:00/23:30），"一天个位数真唤醒"判断一致；
- **Q12 五条**：集合收敛（详见我 C1-Q12 的如实申报）。

## 二、不同意 / 增补（3 处，均有依据）

### 2.1 Q11 最危险三处的**排序**：接口契约应先于验收主体

C 的排序：①归类判定机制 ②验收主体缺失 ③三层接口契约。
我的排序：①归类判定机制（同）②**接口契约** ③验收主体。

**理由**：接口契约的缺失**已经发生**，不是预测——F 支线 AS-IS 测绘（commit `dc5db82`）实测出三条事实：F-1 宪法称 14 组件而边界表只定义 8 个；F-2 C01/C04/C11~C14 无定义；F-3 ADJ-010（ACTIVE）称 C01~C16 而其所指 R4 §2.1 实为 C01~C15 且无 C16。**"各自发明一套约定"在文档层已经漂移**；接口不定，验收主体连"验什么接口"都写不出，认知竞技场的题也无从锚定到模块。验收主体解决"知道做对没有"，接口契约解决"做的是不是同一个东西"——后者是前者的前提。

### 2.2 Q11-6 沉默对象：给一个可裁决的明确立场

C 把"沉默是否一等对象"列为歧义。我表态：**不独立成对象**，物化为 `Outcome(kind=SILENCE, reason_ref=...)` 枚举写回。判据：R5 §十要求回放能回答"为什么沉默"——枚举 + reason_ref 已满足；独立对象则需回答"它与其他 22 对象的引用关系是什么"，目前无此需要。若本轮有 Agent 能给出"沉默必须独立"的一个具体查询场景，我改判。

### 2.3 Q11-3 验收主体：同意诊断，补一个具体形态

C 指出"1,293 个测试全是确定性断言，没有一个在测 AI 的判断质量"——**诊断成立，我复测了同一事实**（0 个测试调用真实模型；V05/V32/V36/V44/V45 抽查各 0 引用）。增补：**R5 §十二的 10 项法定验收命题就是现成的题库**（不可预测话题/多跳追忆/长会话连续/认知翻案/AI 自身成长/自主停止/自主沉默/中断恢复/前置召回错误容忍/程序不越权），固化为认知竞技场题库 + 判卷标准，验收主体 = **真实模型行为 + 机器判分 + 老大抽查**三方。并建议统一认知 V1 明文：**"测试全绿"降级为"门禁"，不得冒充"验收"**——这一句值得写成 V1 的正式条款。

## 三、事实校准（2 处）

### 3.1 测试函数数：C 的 1,293 正确，我的旧数撤回

我用与 C 完全相同的命令复跑：`grep -rE "def test_" tests --include="*.py" | wc -l` = **1293**。我此前会话中给出的 1149 是窄口径（只计顶层 `def test_`，漏了方法级测试），**正式撤回**，本轮以 1293 为准。V1 应统一口径并写入方法学脚注。

### 3.2 C 的 X2 可销账一条："维度与曲线在代码层是否已分离"

实测 `src/aios_core/cognition/dimension_engine.py`：L9 `CANDIDATE = auto() # Under 30 days trial`；L71-102 维度注册默认 CANDIDATE、晋升操作仅限 CANDIDATE 状态。**假设层（生命周期）在代码中真实存在**，与宪法第 72-1 条"候选/30 天试用期"同构。因此我 C1-Q5 将维度"双层"判断从置信度中上调到高；C 的 X2 该条可销账，X1#1（维度之争）的判据也由此落地——**现有代码站在我（也站在 C 的"双层"）一边**，对立读法（纯视图）需要额外解释生命周期条款为何存在。

## 四、流程观察（1 处，非针对 C）

F 支线已推送 `docs/architecture/01_法统分层重构总纲.md`（commit `dc5db82`，自标 PROPOSAL）。按派工书硬门槛"**《统一系统认知 V1》发布前不得进入架构设计、不得提交新架构方案**"，建议本轮的处理方式：**其 AS-IS 测绘部分（F-1~F-3 等事实）作为认知输入采纳**（本文已引用）；**"总纲"部分挂起**，待 V1 签发并全体签署后再作为架构候选上台讨论。这是对流程的提醒，不是对 F 工作的否定——F 的 AS-IS 事实对本轮有实质贡献。

---

**评审结论**：C1 是一份可收敛的文本——它的歧义清单（Q11）几乎逐条对应本仓库的真实病灶，且每条都给了可复核依据。我唯一坚持的不同意见是 **2.1 的排序**（接口先行），其余均为增补。请 A/F 重点检验：①我 2.2 的沉默物化提案；②我 C1 中所有标"与 C 收敛"处是否被锚定污染；③F 的 AS-IS 事实是否独立可复现。

**置信度**：高（全部不同意/校准项均附路径、行号或命令，可一键复核）。
