# 上游对表裁决（2026-09-16 · 合并 `origin/aios-2.0@65a2293` 之后）

> 触发事实：主线上游已被**孤儿重建**（新历史 3 commits，与 `cd8bb29` 无共同祖先），
> 并行会话（派工表战队 `01a0a700/01a0a67a`）的成果已入主干；`TASK_PROGRESS_V3.md`
> 宣布 M0 23 项、M1 20 项"100% 闭环"，M2 攻坚中；`v3.0.1 裁决集 ADJ-001~012`
> 裁决的是**另一份"R4 重构方案"**（§A-3 十二项）——与本分支的《修改案 R4》同号异体。
> 本文件是对这一现实的全部立场修正；此前十二批工件的**内容**不作废，**坐标系**重写。

## 1. 合并事实（本批 commit 为双父 merge，union 精确、零覆盖）

- 策略：文件级并集（双方变更集经 comm 验证互斥：我方 31 文件 vs 上游 93 文件，
  交集为空；14 个 rename 逐一处理，2 个垃圾文件随上游删除）。首试
  `-X theirs` 在无共同祖先下**覆盖了我方 contracts**——当场识破、回滚重做。
  教训入册：孤儿分支合并永远不要带 -X 策略。
- 合并后测试：**960 passed / 0 failed / 4 xfailed**（上游 ~903 + 我方 ~61 全绿）；
  既知环境项 b8 随上游 CI 修复**自然消失**（其 594/1 记录保留在 DEV_LOG，不涂改）。
- 我方根目录 R2 台账被上游迁移吸收（rename 至 `docs/specifications/`），
  arena 增注段不再回填——程序状态主权已在移交备忘录 §5 批次台账，单一事实源。

## 2. 编号冲突表（同号异义，全部登记）

| 编号 | 本分支（R4 程序） | V3 主干（同编号） | 裁决 |
|------|-------------------|---------------------|------|
| M0-023 | source_class 提交分类 + 触发豁免 | 生命安全熔断直通 ≤50ms | 纯编号冲突，语义无涉。**本分支工件一律以"编号+文档路径"双引**；并入主干时重编号（主干已用 M0_CORE 原子分解） |
| M1-017 | 自持倒排检索核 + 物理计划 | CJK 倒排（其 `query/cjk_inverted_index.py`，"三词共现 ≤30ms"） | **同域双实现**。裁决见 §3.1 |
| M1-018 | resolve-before-intersect 消歧 | bi-temporal 复盘派工场景（其升级后的工单） | 我方消歧算子并入检索域；编号让位主干 |
| M1-019 | 世界对象 33.5 tombstone 冷档 | （其"声纹 180 天 TTL 墓碑"属 C01 边缘缓存回收） | **不同法域**：边缘缓存回收不需要 append-only 义务；33.5 管世界历史。两者不互斥，主干未做世界对象侧——缺口保留，见 §3.2 |
| M1-020 | HotCard 热卡管道 | 无对应（其 M1 清单无此件） | 缺口保留，施工图原样有效 |
| G-M1P | 50 万修订 p95 闸 | 无（其 M1 出口无规模闸——F3 病灶仍在） | 我方压测套件是**未认领领土**，见 §3.3 |

## 3. 主干未吸收的独有线（R4 delta 的存在理由，逐条有代码证据）

1. **触发豁免闭环**：grep 证实 `wake/dispatcher.py` 无任何 maintenance/source_class
   处理——R2 永动机（维护写放大成唤醒风暴）在主干**未封死**。我方
   `world_commits.source_class` + 部分索引 + `triggerable_commits_after` 是
   该缺口的完整执法件（含迁移器与 8 用例）。
2. **runtime_profile / 信任字段**：主干 contracts 无 `ProfileName/IngestPolicy`，
   无 `source_trust/corroboration_required`（其 models 未动）——M0-030 与
   R4-09.1 原样有效，且其"1 秒首字/摄入硬约束"（M7-002 30ms 宣称）**无 profile
   契约支撑**，正是 §2.4 要防的"按虚拟假设过度优化"。
3. **世界对象归档纪律**：33.5 tombstone + 防删静态守卫（R4-07a）+ 019 施工图；
   主干仅在 ingest 层有 is_tombstone 标志。
4. **G-M1P 规模闸与三点位一致性**：其 M1 已宣布闭环但从未在 50 万修订下测过
   检索（F3"验尸时才检查心脏"原病灶）；其 M7-002 排期在压力测试前置——我方
   套件恰好是它 M7 缺的工具。移交建议：把 `aios_core.bench.g_m1p` 提给主干
   作 M7 预演工具，其 cjk 索引与我方核同场跑三点位（互为基线，±20% 口径）。

## 4. 双实现裁决（检索核）

**主干优先**（903 用例背书 + 已接入其 M1 消费面）；我方 `query/search.py` 转为：
① G-M1P harness 的参考实现（bench 现在指我方核——保留，因它同时是压测对象的
"对照组"）；② 别名消歧/水位双态的**语义供给源**（其索引若缺 stale_index
拒绝装新、ambiguous 不自动合并，对表数据会当场显形）。不做吞并主张。

## 5. 治理工件状态修正

- 签核包：批准/驳回动作对象从"单一主线仓库"改为"V3 总指挥部是否接收 R4 delta"；
  复审块 B/C 全部保留效力；§2 第 3 步台账行改指本文件。
- `POST_GATE_48H`：前提（M1 未开工）**失效**；降格为《R4 独有缺口施工检查单》——
  B2 检索服务层删除（主干已有消费面），保留 B1 彩排、019a/b、020a、压测终跑、
  m2_unlock 语义（改述：主干 M2 攻坚中，则 F3 红线应以"M7 前补规模闸"重述）。
- 移交备忘录 §1 现状句以本文件为准（版本化修正，不涂改原文）。
- 闹铃仍有效：M1-016 B 半 4 用例中"双透镜/热卡/prune"三条主干同样未实现
  （其 `retrospective_annotation` 已部分覆盖双透镜——对表后可能 XPASS，
  届时按闹铃规则摘标并致谢主干）。

## 6. 本批保留的既有承诺之变更审计

唯一被现实改掉的承诺：48h 时刻表的"B0 从签核开始"——签核程序已被 V3 的
G0 门+ADJ 裁决集流程事实取代。**没有**被改掉的：红线 1–5、刻意不做清单、
投影纪律、防删守卫、CAM 编号纪律（现在多了一课：连 R4 这个编号本身都会撞车）。
