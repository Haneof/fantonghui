# 《AIOS 全流程海量盲测与极限压测报告》

**归档线**：MEGA-PIPELINE-001 ｜ 基线 commit：8f5f21a + 增量层
**考台**：`src/aios_core/simulation/total_pipeline_bench.py`（TotalPipelineBench）
**语料**：MassiveLifeBenchGenerator（SIM-MASSIVE-001，seed=42，对抗发生，非自编自答）
**压测规模**：20,000 世界对象（report 快照：`world_revision=44`；装载 4.47s）

## 一、总账

| 指标 | 实测 |
|---|---|
| 8 阶段总墙钟 | **28.77s** |
| 大模型调用总数 | **4 次**（常数级，非雪崩） |
| tracemalloc 峰值驻留 | **173.8 MB** |
| 亿级外推说明 | 20k→1M 线性外推主要在持久化与索引重建两项，详见诊断书 |

## 二、分阶段延迟分布（wall / P50 / P95 / P99，ms）

| 阶段 | wall_ms | P50 | P95 | P99 | 结论 |
|---|---|---|---|---|---|
| S1 摄入清洗 | 1096.48 | 23.09 | 23.09 | 23.09 | 全量读面扫噪声占大头，压缩本体 23ms |
| S2 时间金字塔 | 4.33 | 0.27 | 1.25 | 1.25 | 四层结晶+下钻毫秒级 |
| S3 多维共振 | **21998.80** | 5408.07 | 5423.74 | 5423.74 | **唯一秒级瓶颈：索引全量重建** |
| S4 高阶演进 | 5.66 | 0.00 | 0.00 | 0.00 | 认知层导数常数级 |
| S5 单跳隔离 | 4568.55 | 7.21 | 7.21 | 7.21 | 20k 行 SHA 指纹 python 级扫=主耗 |
| S6 硬核建议 | 21.46 | 0.00 | 0.00 | 0.00 | 三顾问毫秒级出建议 |
| S7 人设防线 | 7.50 | 0.00 | 0.00 | 0.00 | 防线判定常数级 |
| S8 驾驶舱 | 17.99 | 0.00 | 0.00 | 0.00 | P0 旁路 0.042ms wall（回执 0.0024ms） |

（逐项细节见 `/tmp` 会话快照 mega_report.json 的等值重放在
`tests/e2e/test_total_pipeline_8stages.py::test_full_pipeline_ledger_and_report_shape`。）

## 三、五大最高铁律捍卫账本（100% 通过，关键断言实录）

1. **铁律 1（输出质量绝对第一）**：S6 三顾问建议 `evidence_min=1`（无证据即拒收）、
   S8 十轮终极对话 `sentence_counts=[1,2,1,2,2,1,1,2,1,1]`，单轮全部 ≤3 句。
2. **铁律 2（历史绝不篡改）**：S5 `history_fingerprint_stable=true`
   （20k 行 SHA-256 前后一致）、`single_hop_only=true`（stale 仅 ["B"] 一跳）、
   `llm_calls_constant=1`（无 210 次雪崩）、`annotated_overlay=1`（双透镜看到注记）。
3. **铁律 3（P0 硬旁路）**：S8 `p0_status=SAFETY_BYPASS_EXECUTED`、
   `p0_llm_calls=0`、`p0_receipt_latency_ms=0.0024` ≤ 50ms 红线。
4. **铁律 4（物理删除冗余）**：S1 `noise_physically_deleted_bytes=30760`、
   `sink_retained_bytes=0`（原始字节保留量归零）、`collapse_ratio=160.06`
   （2881 拍 → 18 条记录，突变拍 16 拍逐拍永存、IMU 冲击独立成件）、
   `core_evidence_preserved=5`（借款/判决/合同/承诺/诈骗五词面全保）。
5. **铁律 5（三闸门）**：S4 `immature_blocked=2`（<3 天申请 100% 拒）、
   `quota_one_per_day_enforced=true`、`inflection_fuse_armed=true`
   （加速度拐点日 4 提前熔断武装）。

## 四、门禁结论

- 测试面：`tests/e2e/` 17 件（8-stage 9 件 + ToolProposal 8 件）全绿；
- 全量仓库面：本报告随交付一并跑通（见随附 commit 说明）；
- 本报告任何数字可由 `TotalPipelineBench(scale=20000).run_all()` 实锤重放，
  无任何写死 mock 自证场景。
