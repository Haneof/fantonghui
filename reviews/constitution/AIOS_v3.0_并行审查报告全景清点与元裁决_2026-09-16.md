# AIOS v3.0 并行审查报告全景清点与元裁决（Meta-Adjudication）

- **裁决人**：`arena/01a0a631-fantonghui` 会话（本分支两份报告作者之一，**利益冲突已声明**：对本分支报告的评分与采信，请按下调一档理解）
- **裁决日期**：2026-09-16
- **基线**：`cd8bb29`（v3.0 宪法提交点），代码状态实测 **558 passed**
- **清点方法**：`git fetch origin '+refs/heads/*:refs/remotes/origin/*'` 拉取全部 11 个远端分支 → `git ls-tree -r -l` 逐分支枚举文件与字节数 → 逐份 `git show` 读取裁决与评分行 → 对报告中可验证的事实性数字**在本沙箱复算**（测试数、冻结对象数、条款计数、分词行为）
- **本裁决不修改系统定义，不构成总工程师 FINAL_PASS**

---

## 一、清点结果：15 个文档条目（16 个物理文件）/ 4 个分支

**同类报告确实很多。** 按分支逐一 `git ls-tree` 清点，排除 M0 时代的 `reviews/M0/*` 与 `governance/agent_reports/m0-parallel-audits/*` 后：

> **16 个物理文件 = 15 个文档条目**（差 1 是因为 `EXECUTIVE_SUMMARY_2026-09-15` 有 `.md` 与 `.html` 两种格式，同一份汇总件）。
> 按性质分：**6 份宪法评审 + 5 份断层审计 + 4 份元审查/汇总件**。
> 另有 **1 个可复跑探针脚本 + 1 份实测结果 JSON**（未计入上表）。
> 本报告以“文档条目”为单位编号 #1~#15；下文引用的编号一律指本表。

| # | 分支 | 文件（字节） | 类型 | 裁决 | 四维评分 | 证据等级 |
|---|---|---|---|---|---|---|
| 1 | 01a0a46e | `reviews/AIOS_Constitution_v3_chief_review_2026-09-15.md`（15,371） | 宪法评审 | 方向对、护栏不及格 | **8 / 6 / 7 / 5**（第 131-134 行） | 文档级 + 沙箱 pytest |
| 2 | 01a0a46e | `reviews/AIOS_v3_gap_audit_2026-09-15.md`（11,875） | 断层审计 | **M2 卡死**，G1–G14 | — | 文档级（带行号） |
| 3 | 01a0a46e | `reviews/AIOS_v3_parallel_reports_meta_review_2026-09-15.md`（4,468） | 元评审 | 10 份清点、可信度排序 | — | 转引 + 自我认账 |
| 4 | 01a0a631 | `reviews/architecture/AIOS_v3.0_CHIEF_REVIEW_PATCH_REQUIRED_2026-09-15.md`（56,785） | 宪法评审 | **PATCH REQUIRED** | **8 / 5 / 6 / 5 = 6.0** | **可复跑探针 + 360 万行实测** |
| 5 | 01a0a631 | `reviews/architecture/AIOS_V3_ENGINEERING_GAP_AUDIT_PATCH_REQUIRED_2026-09-16.md`（57,285） | 断层审计 | **REALIGNMENT REQUIRED**，41 项断层 / 28 新 Issue | — | 文档级 + 引用探针 |
| 6 | 01a0a631 | `reviews/constitution/AIOS_v3.0_独立评审报告_2026-09-15.md`（61,911）**（本会话）** | 宪法评审 | **REVISION_REQUIRED** | **8.5 / 4.0 / 6.0 / 6.5 = 6.3** | 实测（探针脚本现已入库，见 §五） |
| 7 | 01a0a631 | `reviews/constitution/AIOS_v3.0_工程文档横向对齐断层审计_2026-09-16.md`（48,734）**（本会话）** | 断层审计 | **M1 静默偏航 / M2 机制性卡死**，30 项断层 | — | 文档级（带行号）+ 关键词矩阵 |
| 8 | 01a0a631 | `reviews/constitution/CONST_V3.0_CHIEF_REVIEW_2026-09-15.md`（48,227） | 宪法评审 | **CONDITIONAL PASS** | **9 / 6 / 8 / 7 = 7.5** | 文档级 + 与 #4 交叉核验 |
| 9 | 01a0a631 | `reviews/constitution/CONST_V3.0_GAP_AUDIT_2026-09-16.md`（28,798） | 断层审计 | **M2 死锁 / M4 功能性崩塌** | — | 文档级 + 全文 grep 实测 |
| 10 | 01a0a632 | `reviews/v3.0/AIOS宪法v3.0_首席架构师严苛审查报告_2026-09-15.md`（27,433） | 宪法评审 | Conditional Pass | **9.2 / 5.5 / 8.5 / 6.8** | 文档级 |
| 11 | 01a0a632 | `reviews/v3.0/V3宪法与工程文档Gap横向对齐审查_2026-09-15.md`（30,048） | 断层审计 | **M0 部分卡 / M1 硬卡 / M2 彻底卡 / M4 假通过** | — | 文档级 |
| 12 | 01a0a632 | `reviews/v3.0/EXECUTIVE_SUMMARY_2026-09-15.md`（4,671）+ `.html`（5,932） | 汇总件 | 7.5/10 | — | 二手 |
| 13 | 01a0a632 | `reviews/v3.0/META_REVIEW_4并行模型共识裁决_2026-09-15.md`（11,336） | 元评审 | 综合 7.0 Conditional | — | 转引（未复验实测） |
| 14 | 01a0a633 | `AIOS核心系统宪法v3.0_首席架构评审报告_2026-09-15.md`（53,560，仓库根目录） | 宪法评审 | **Conditional Pass** | **9.0 / 6.5 / 8.5 / 8.5** | 文档级（部分数字系转抄，见 §四-4） |

**辅助证据**：`reviews/architecture/evidence/aios_v3_sqlite_probe.py`（18,737 B）+ `..._3_6m_result.json`（2,598 B）——**全批次唯一可复跑的量化证据**；本会话的三支探针脚本已于本次提交补入 `reviews/constitution/evidence/`（见 §五）。

**清点更正**：#13（01a0a632 META）称“合计 10 份 / 4 分支”、#3（01a0a46e META）称“10 份 / 5 个独立模型”、#15 称“物理上可见 7 个 V3 review Markdown”——**三份元件的数字都不适用于全仓当前状态**（前两份已过期，#15 只覆盖 01a0a631 单分支的 V3 主审/断层审计）。按当前远端实际状态为 **16 个文件 / 15 个文档条目 / 4 个分支**（不含 M0 时代的 `reviews/M0/*` 与 `governance/agent_reports/m0-parallel-audits/*`，那些是 M0 工程裁决，不是 v3.0 宪法审查）。

| # | 分支 | 文件（字节） | 类型 | 裁决 | 证据等级 |
|---|---|---|---|---|---|
| 15 | 01a0a631 | `reviews/architecture/AIOS_V3_MULTI_REVIEW_META_AUDIT_NO_GO_2026-09-16.md`（34,499，提交 `493a3e0`） | **元审查 + 统一裁决** | **`PATCH_REQUIRED / AS-WRITTEN NO-GO`** | 逐份质量审查 + SHA256 快照 + 独立性分析 |

> **#15 是本批次目前最重要的一份文件**，原因不在裁决（结论与前 14 份同向），而在它做了两件别人没做的事：①给 6 份源报告打了 **SHA256 不可变快照**（并单独保存 C 的首发版哈希，防止后续合并污染原始比较）；②**证伪了“多份独立盲审”这个前提**（见 §2.3）。它的覆盖面比本报告窄（只纳入 01a0a631 分支的 3 份主审 + 3 份断层审计），但纵深比本报告深。**两份元审查互补，不重叠：#15 深审 6 份，本报告广清 15 个条目。**

---

## 二、共识：15 份文档收敛度极高，这部分可以直接当结论用（但收敛 ≠ 独立，见 §2.3）

**6 份宪法评审 + 5 份断层审计 + 4 份元审查/汇总件。** 11 份实体评审全部落在 “Conditional Pass / Patch Required / Revision Required / Realignment Required” 区间——**没有一份给无条件 PASS**；#15 的统一裁决更进一步，给出 **`AS-WRITTEN NO-GO`**（v3.0 文本不得作为可直接执行的冻结合同，但方向与目标架构保留）。方向层面，全员一致认定“愿景对、当前文本不可施工”。


### 2.1 全员命中（可直接写进 v3.0.1 修宪案，无需再议）

| 共识项 | 命中报告 | 收敛后的唯一正确解 |
|---|---|---|
| **第 31 条之一「倒带加注」vs 第 93 条「历史不可篡改」** | 1/4/6/8/10/14 全部 | 追加式 Annotation/Claim 对象：`valid_time=过去`、`learned_at/recorded_at=T_now`、指针指向旧切片；**物理 Observation 一个字节都不改**；读侧提供 `AS_OF(t)` 与 `AS_KNOWN_AT(cutoff)` 两种视图 |
| **第 25 条「永存」vs 第 33.5 条「大模型坚决物理删除」** | 1/4/6/8/10/14 全部 | 分级保留（原始环形缓冲 TTL → 取证层 → 长期语义层）+ **DeletionLog/Tombstone 审计** + EventAnchor 证据与关键原话白名单不可删 + 清洗误删率回归测试 |
| **第 78 条「Wake Reason 第一指针」vs 第 84 条「四步序先照镜子」vs 第 80 条「方便度第一研判」** | 1/4/6/8/10/11/14 | 四步序 = **单 Prompt 内的段落组装顺序 + 四个必备决策因子**，禁止串行多次模型往返；安全与当前输入置于 Step 0；语调不得先于事实裁决 |
| **维度/共振无预算 → 维度爆炸** | 1/4/6/8/10/13/14 | 候选上限 + 晋升阈值（共振计数 ≥N 且跨源 ≥2）+ 退场 TTL，全部落在 `DimensionDerivation` 状态机上；金字塔密度按维度声明最小粒度 |
| **修正传播：禁止 eager 全链重算** | 1/2/4/5/6/8/13 | eager 一跳标记（廉价）→ 预算化懒重算 → **读时有效性校验**；深度/宽度/预算/熔断四道闸 |
| **共现检索必须走倒排 + 中文分词** | 4/6/7/9/10/11/14 | 关键词抽取 + 别名表 + 倒排/FTS（**预分词**）+ 三因子打分 + top-k + 时间窗预裁剪；p95 SLO |
| **5D 滑动条必须读物化层** | 1/4/6/7/9/14 | 宏观档读预聚合 rollup，下钻才分页读原始层；无数据桶不得创建空总结 |
| **「1 秒首字」必须 SLO 化** | 1/4/6/8/10/13/14 | 定义计时起点（用户输入结束 → 首 token 到端）、上下文硬预算、p50/p95、L0/L1/L2 分层、网络抖动三档基线 |
| **三级流式流水线 = 0 个 Issue** | 2/5/7/9/11 全部断层审计 | 新增前台活跃窗口 + 后台增量萃取（带 watermark 与幂等键）+ 联想回捞三类任务 |
| **条件任务引擎缺失 → Token 空转** | 2/5/7/9/11 全部 | `TriggerExpression` 一等条件对象（三值求值 + 事件订阅索引 + ready queue + TTL），复用 M2-007 已有的 Watch DSL |
| **测试规范停在 V01~V20，V21~V30 与 R3/V3 验收门全缺** | 5/6/7/9/11/14 | 补 V21~V30 + 脏数据场景 + 反长篇/黑盒/1~3 句验收门；**且必须在开工前冻结**（测试规范 §16 禁止事后改标准） |
| **三个一等对象零契约（任务书 §1 冻结 19 个 vs 宪法第 71 条 22 个）** | 5/6/7/9 | 补 M0-023/024/025：`Prediction` / `LifeChapter` / `CommunicationExperience` |

> **措辞边界（采纳 #15）**：表中“0 个 Issue / 缺失”一律指**任务书与验收层面没有对应 Issue、owner 与验收门**，**不等于代码层毫无能力**。#15 依 #5 的语义审计指出：`workspace.open` 已是 Single-Shot Manifest 的雏形、`world.search` 已支持实体/时间/模态过滤、M2-007 的 Watch 已有有限 DSL、M2-012 的自主工具循环不违宪。修宪时的正确动作是**补契约、补 owner、补验收**，不是推倒重写这些已有实现。
>
> 上述 12 项**已由本裁决逐条核对原文，且可复算项已复算**（19 vs 22 已核对任务书 §1 表格行数 = 19 行；宪法第 71 条 = 22 行）。**可以直接作为 v3.0.1 修宪包的输入，不需要再开一轮评审。**

### 2.2 评分分布：一致的方向，分歧的幅度

| 维度 | 最低 | 最高 | 极差 | 中位 |
|---|---|---|---|---|
| 1 架构前瞻性与理论深度 | 8.0（#1/#4） | 9.2（#10） | 1.2 | **8.5** |
| 2 工程落地与实现可行性 | **4.0（#6 本会话）** | 6.5（#14） | 2.5 | **5.75** |
| 3 穿戴物理形态契合度 | 6.0（#6 本会话） | 8.5（#10/#14） | 2.5 | **7.65** |
| 4 规则完备性与防退化能力 | 5.0（#1/#4） | 8.5（#14） | **3.5** | **6.9** |

**唯一无争议的结论：第 2 维（工程可行性）是全员最低分，第 1 维（前瞻性）是全员最高分。** 也就是说——**15 份文档条目一致认定“方向对、护栏不及格”**。这条可以直接进管理决策。

---

### 2.3 但这不是 6 次独立盲审——本报告原先的表述已被 #15 证伪，现予更正

本报告初稿写的是“6 个独立会话、无串通的前提下，这是强信号”。**该表述不成立，已删除。** #15 用 git 提交与正文自述证明了明确的信息继承链：

| 报告 | 首发提交 | 与前序报告的关系（#15 核实） |
|---|---|---|
| A = #4 | `cf4e9f6` | 首份主审，附探针 |
| B = #6（本会话） | `5182ef3` | **正文第 9~20 行明确写明已知 A 并交叉核验其探针** → 互补复核，非盲审 |
| C = #8 | `3c4b641` | 原始主审，后由 `10a6351`、`d0b6217` **两次增补吸收 A/B** |
| D = #5 | `46182bc` | 首份下游断层审计 |
| E = #7（本会话） | `be25bce` | **正文明确交叉核对 D**，并声明增量发现 → 非盲审 |
| F = #9 | `0adecfe` | 首发后由 `ff0adff` **增补吸收 D/E**，已从原始审计变为“原始意见 + 合并说明” |

**正确表述（采纳 #15）**：*多名评审者反复复核后高度收敛，其中有两组中文检索实测互相印证；但结论链存在继承，不能按独立样本数做统计投票。*

**连带更正**：#8 中“三份审查互不通气”“三轮独立印证”、#9 末尾“四方独立”“三份审计独立完成”等表述**均应降级**。同理，#3 与 #13 两份元评审中“N 个独立模型共识”的统计口吻也不成立。**收敛的价值依然真实——它说明反复复核没有推翻结论——但它不是独立性证据。**

## 三、分歧：五处真实争议，逐条裁决

共识不值钱，分歧才需要裁决。以下是我核对原文后认定的**真实分歧**（不是措辞差异）：

### 争议 1：M2-012 的“多轮工具循环”是否违宪？——**#2 对，本会话（#7）过判**

- #7（本会话）在断层表里把 M2-012「模型可多轮 tool calls」标为**正面冲突**。
- #2（01a0a46e 断层审计）明确判：「**工具内循环本身不违宪（V3 禁的是多轮元提示词，不是 tool loop），此项不按旧式逐步对话设计，不构成冲突；冲突点仅在启动序与姿态缺失**」。
- **裁决：采纳 #2。** v3.0 第 84.1 条禁的是“系统发提示词 → AI 说好的我知道了 → 再发下一段”这类**元对话往返**，第 91 条自己就给了 30+ 原子工具，必然产生工具循环。#7 的表格属**过判**，其修补建议（“明确单次看盘指上下文一次性组装，不禁止工具循环”）反而是对的——**表格与建议自相矛盾，以建议为准**。
- **连带影响**：#7 的 GAP-07 应从“正面冲突”降级为“启动序冲突”，严重度不变（仍是 S1，因为四步序确实与十三步顺序相反）。

### 争议 2：M0 到底卡不卡？——**四份报告措辞不同，实质一致**

| 报告 | 措辞 |
|---|---|
| #11（01a0a632） | M0 **部分卡死**（Observation 缺 `source_confidence/signal_quality/voiceprint_lifecycle`；Task 缺 `TriggerCriteria`） |
| #5（01a0a631） | M0 **审计阻断点**，冻结门不得放行 |
| #9（01a0a631） | M0 **基本免疫**，但必须补 M0-023~025 |
| #7（本会话） | M0 **自身的门能过，但冻结不应放行** |

- **裁决：实质一致——M0 已冻结的代码不需要返工，但 schema 冻结动作不应放行。** 理由是硬的：任务书 §1 冻结对象 **19 个**，宪法第 71 条 **22 个**，差的正是 `Prediction` / `LifeChapter` / `CommunicationExperience`；而错误码 `BUDGET_EXHAUSTED`（`src/aios_core/contracts/enums.py:169`）与 `STALE_INDEX`（同文件 168 行）已冻结，**错误有出口、机制无入口**。
- **可执行结论**：M0 补 3 个契约 Issue（M0-023/024/025）+ 2 个升级（Observation 加 `source_quality` 计算口径与 `min_granularity`；公共字段加双时态与 `annotates`），**然后才放行冻结**。已通过的 M0-003~021 不受影响（#8 已核对为增量兼容）。

### 争议 3：第 4 维评分 5.0 vs 8.5（极差 3.5）——**两边量的不是同一件事**

- 给 5 分的（#1/#4）在量**条款自洽性**：数出 12~13 处互斥条款，认为“宪法自己打自己”就是防退化能力不足。
- 给 8.5 分的（#14）在量**制度完备性**：18 条一票否决 + 15 条基础契约 + 三级修宪 + 12 要素 Issue + 7 向归因，且与 M0 实际执行（REOPENED/PATCHED/独立复审）互相印证，“是真在用的制度而非摆设”。
- **裁决：两边都对，指标该拆。** 建议 v3.0.1 评审模板把第 4 维拆为 **4a 条款自洽性** 与 **4b 治理制度完备性**。按此拆分，本批次共识约为 **4a ≈ 5.5、4b ≈ 8.5**——**问题全在条款打架，不在制度缺失**。这个区分很重要：它决定了修宪方式是“出补丁包”而不是“重写治理框架”。

### 争议 4：两份元评审互相报错了对方的分数——**已复算，#13 错**

- #13（01a0a632 META）写 01a0a46e 的评分为 **7/6/7/5**。
- #3（01a0a46e META）写自己为 **8/6/7/5**。
- **复算结果**：#1 原文第 131-134 行评分表为 **8 / 6 / 7 / 5**（“理论自洽性 7/10”是该章小结，不是四维评分）。**#13 把章节小结当成了维度分。**
- **裁决：#1 的四维分以原文 8/6/7/5 为准；#13 的表格需更正。** 这不是小事——**元评审是唯一会被管理层直接采信的一层，它报错分数就会污染决策**。

### 争议 5：谁是“最可信”的报告？——**证据等级差异极大，且有一处数字是抄的不是测的**

- **唯一带可复跑代码的是 01a0a631 的探针**（`aios_v3_sqlite_probe.py` + 360 万行结果 JSON）。其余 13 份全部是文档级论证。
- **发现一处转抄错误**：#14 称“**418 项测试全绿**”。本裁决在 `cd8bb29` 基线实测 `PYTHONPATH=src pytest` → **558 passed**。`418+15` 这个数字在 `TASK_PROGRESS_R2.md` 中出现 **22 次**——**#14 是从进度表抄的，不是跑出来的**，且该表本身已过期。
- **裁决**：
  1. **采信排序：带可复跑证据 > 带行号引文 > 纯论证。** 具体为 **#4/#5（探针）≈ #6/#7（实测 + 行号）> #9（全文 grep 实测）> #2/#11（行号）> #8/#10/#14（论证 + 交叉核验）**。
  2. **任何进入修宪案或 Gate 的数字，必须能被复算**。抄进度表、抄别的报告，一律退回。
  3. **本会话自认一项**：#6/#7 的基准数字此前只存在于报告正文，**探针脚本没有入库**——与 #4 相比是可复现性缺陷。本次已补齐（见 §五）。

---

## 四、我的审查意见（作者为当事方之一，按 §2.3 不属独立盲审）

### 4.1 这批报告的质量判断

1. **方向判断可信度：高。** 15 份文档在“哪个里程碑出事”“哪几条条款打架”“哪些机制零任务”上完全收敛，且收敛点全部能被原文与实测复核。**这批报告可以放心当作 v3.0.1 的输入。**
2. **数量已经过剩，边际收益转负。** 从第 4 份开始，新增报告基本在重复前 3 份的结论，只是换编号体系。已核实的编号体系至少 **6 套**：`G1~G14`（#2）、`C-01~12 / R-01~19`（#4）、`41 项 / GOV-01~04…`（#5）、`F1~F12 / R4-A~C`（#8）、`GAP-01~16`（#11）、`GAP-01~30`（#7 本会话）。**其中 #7 与 #11 前缀同为 `GAP-` 但指向完全不同的断层——这不是“不兼容”，是编号冲突**，派单时引用 `GAP-05` 会产生二义。这本身已成为新的工程风险。
3. **最缺的不是再一份评审（第 15 份已经到了，是 #15 的统一裁决），而是三件收尾工作**：①统一编号；②把可复算证据入 CI；③按 v3.0.1 出**一份**修宪补丁包。

### 4.2 对本会话两份报告的自我更正：#15 对报告 B 提了 7 项批评，我逐条核了原文

#15 §2.2 对本会话的 #6（报告 B）列出 7 项“夸张或证据不足”。我逐条回到原文核对，结论如下——**6 项完全接受，1 项部分接受**：

| # | #15 的批评 | 我的核对与裁决 |
|---|---|---|
| 1 | “10 项新机制零覆盖”过度依赖关键词 grep；`workspace.open` 已是 Single-Shot Manifest 雏形、`world.search` 已有实体/时间/模态过滤、Watch 已有有限 DSL、M2-012 工具循环不违宪 | **接受。** 正确表述是“**局部能力存在，但 V3 契约、owner 与验收不完整**”，不是“全部从零”。本会话 #7 已部分自我修正（承认 M2-011 与工作台 §9.3 隐含 Single-Shot 方向、M2-007 的 Watch 已能表达定时触发），但 #6 的“零覆盖”措辞过强，**以 #7 与 #15 的口径为准** |
| 2 | “100K 上下文与 1 秒物理不可能”过于绝对 | **部分接受。** 窄命题成立且有算术支撑（#6 第 100 行：4K prefill ≈ 100~300 ms，线性外推 100K ≈ 2.5~7.5 s；第 68/108 行据此判 100K 级 prefill 与 1 秒首字不可共存）；**但 §2.1 的标题式绝对表述“1 秒首字在当前条款下不可达”未绑定模型/硬件 profile，应改写为“在未给定模型、硬件与分层预算前，1 秒首字不可承诺”** |
| 3 | 50 万/100 万实测未随报告提交独立脚本与机器可读结果，复现级别低于 A | **接受，且本轮已修**：三支探针脚本已入库（见 §五）。但 #15 说得对——**#6 落库时确实没有** |
| 4 | 年 Token/存储成本是情景估算，不是观测值 | **接受。** 它的正确用途是**驱动 Budget Gate**，不是当唯一容量预测。维度数/摘要长度/调用频率/单价任一变化，结论就变 |
| 5 | “`BudgetLedger`/`ContextSnapshot` 必须成为核心 WorldObject”尚未证明 | **接受。** 能力必须有，但可先实现为可审计控制面记录或不可变会话 envelope；**是否升格为第 71 条的 22+ 个领域核心对象，应由 schema ADR 决定**，不应由评审报告直接定 |
| 6 | “声纹只允许合并、永不淘汰”太硬 | **接受，且这是 #15 对我最有价值的一条修正。** 原文确在（#6 第 218 行、#7 第 324 行均写“声纹只允许**合并**，不允许**淘汰**”）。正确解是 #14 的：**人物身份连续性用稳定 Speaker ID + 墓碑保证，但生物特征模板本身必须支持权限撤回、轮换、合规删除与 tombstone**——身份连续性不能绑死在永久保存声纹上 |
| 7 | 若干“业界没有同等能力/本结构领先”依赖博客或不完整对比 | **接受。** Letta/Mem0/Zep 的对标材料是博客级；只有 Generative Agents（arXiv 2304.03442）是论文级。**博客级材料不得作为施工 Gate 的事实依据**，只能作为设计参考 |

**这 7 条不改变 #6/#7 的总裁决方向（`REVISION_REQUIRED` / M1 偏航→M2 卡死），但把 3 处措辞从“断言”降为“待证”，把 1 处方案（声纹）替换为更好的解。** 我在 §三 争议 1 中已认下一处过判（M2-012），加上这里的 7 项，本会话两份报告共有 **8 处需要按 #15 口径修正**。

### 4.3 对管理层的三条硬建议

1. **立刻停止再开新的并行评审会话。** 现有 15 份文档的共识部分已足够；继续并行只会增加编号混乱与互相转抄错误（已抓到 2 例：#13 报错分、#14 抄测试数）。
2. **把 #15 认定为统一裁决的事实底稿，剩下的工作只有两件**：①**编号统一**——把 `G1~G14` / `C-01~12` / `41 项` / `F1~F12` / `GAP-01~16` / `GAP-01~30` 六套合并为一套（建议以 #15 的 A~F 报告代号 + 单一 `V3G-xxx` 断层号为准，优先解决 `GAP-` 前缀冲突）；②出一份 `v3.0.1 修宪案` + 一份 `Issue 增补表`，以 §2.1 的 12 项共识为骨架、#4/#5 的 Gate 结构（`V3-GATE-001/002/003`）为容器，把 §4.4 的独有贡献作为子条挂进去。**#15 已完成“统一裁决”，不要再开第二份元审查。**
3. **把 4 支探针并入 CI 作为 M1 Gate 硬门**：`aios_v3_sqlite_probe.py`（#4 的 360 万行规模门）+ 本会话 3 支（`probe_500k_schema_search_5d.py` 索引/共现/5D 分桶、`probe_fts5_vs_postings.py` 检索方案对照、`probe_cjk_tokenizer.py` 中文召回 > 0 的硬断言）。**这 4 支是全部 15 份文档里唯一能把“意见”变成“回归测试”的东西**，不用等于白审。

### 4.4 各报告独有的、值得保留的贡献（避免合并时丢失）

| 报告 | 独有贡献（其他报告 0 命中） |
|---|---|
| #1（01a0a46e） | **provenance 置信代数**（`observation/external/inference/introspection` 分类 + 乘法折扣，禁止内省产物重置为高置信）——治“认识论洗钱”的最优雅解 |
| #4（01a0a631） | 360 万行可复跑探针；`SourceEnvelope` / `InstructionTrust` 信任域分层（治间接提示注入与记忆投毒）；L0/L1/L2 延迟分层 |
| #6/#7（本会话） | ①**A01~A10 验收编号在 v3.0 与架构规划中一物两义**（M2-015 引用二义）；②**工作台规格 §7.2「待办任务必须进入定期任务盘点」= 把 Token 空转制度化**；③**M1-001 验收「10k 条模拟心率可批量写入」与第 33.1 条正面冲突**；④**测试规范 §16「不得看完盲测结果后修改标准」造成的修补时间窗口**；⑤金字塔年成本模型（≈3×10⁷ token/年/用户）；⑥`BudgetLedger` 与会话上下文快照作为一等对象 |
| #8（01a0a631） | R4-A/B/C 三包修宪结构；“LLM 判断单点化”这一根因归纳；与 #4 的逐条交叉核验表（同行复核范式） |
| #9（01a0a631） | 文档-条款-Issue 三级精确定位；“三方顺序冲突”表述；M4 功能性崩塌判词 |
| #10（01a0a632） | 按模态分桶合并 Wake；维度晋升需 p_value/effect_size |
| #11（01a0a632） | “M4 假通过真失败”这一最危险的管理陷阱命名 |
| #14（01a0a633） | 声纹**墓碑 + 回归重识别协议**（淘汰 ≠ 遗忘）；point-in-time 查询成本悬崖与“只物化局部子集”；四步序 = 单 Prompt 段序的**延迟中性**论证；清洗误删率 ≤1% 的量化回归门；音频链路（ASR→TTS 流式、双工打断、VAD）盲区 |

### 4.5 一句话总评

> **15 份文档、4 个分支，结论收敛到同一句话（收敛来自反复复核，不是独立盲审——见 §2.3）：v3.0 的方向是这批文档里最值钱的东西，工程护栏集体不及格；卡死点在 M2，静默偏航点在 M1，债务放大点在 M3。**
> **现在真正稀缺的不是第 16 份审查意见，而是把 6 套编号合并成 1 套、把 4 支探针塞进 CI、出 1 份 v3.0.1 补丁包。**

---

## 五、本次裁决附带的可复现性修补

本会话此前两份报告（#6/#7）的基准数字只在正文中，未入库。本次一并补齐，使本分支证据等级与 #4 对齐：

```
reviews/constitution/evidence/probe_500k_schema_search_5d.py   # 50万/100万行：索引点查、LIKE 全表共现、时间窗裁剪、5D 分桶
reviews/constitution/evidence/probe_fts5_vs_postings.py        # 同语义查询对照：LIKE 319ms vs FTS5 4.2ms vs 倒排 31.3ms
reviews/constitution/evidence/probe_cjk_tokenizer.py           # unicode61 对连续中文 0 命中；trigram 对 2 字词 0 命中
```

复算记录（本沙箱，SQLite 3.40.1）：

| 项 | 实测 |
|---|---|
| 仓库测试基线（`cd8bb29` + 本分支文档提交） | **558 passed**（`PYTHONPATH=src pytest`） |
| 任务书 §1 冻结对象行数 / 宪法第 71 条对象数 | **19 / 22** |
| 三词共现（无索引）50 万行 / 100 万行 | **319 ms / 654 ms** |
| 同语义 FTS5（预分词）/ 倒排表 | **4.2 ms / 31.3 ms** |
| 5D 全表月分桶 100 万行 | **369 ms** |
| 连续中文 `MATCH '妈妈'/'生日'/'礼物'` | **0 / 0 / 0 命中** |

---

*本元裁决由当事方之一出具，已声明利益冲突；按 `reviews/README.md` 规则，FINAL_PASS 只能由项目总工程师裁决，本报告不创建任何 FINAL_PASS。建议本裁决连同 §2.1 共识清单交由非当事方复核后，作为 v3.0.1 修宪案的唯一输入。*
