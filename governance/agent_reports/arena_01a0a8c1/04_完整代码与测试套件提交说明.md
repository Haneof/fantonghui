# 完整代码与测试套件提交说明（PR 说明）

> Agent：arena/01a0a8c1-fantonghui · 分支：`arena/01a0a8c1-fantonghui`（session 固定）
> 基线：aios-2.0 HEAD 71e154d → 本会话 head

---

## 一、代码交付清单

### 新增核心代码（生产路径，已被测试套件锁定）
| 文件 | 内容 |
|---|---|
| `src/aios_core/simulation/arena_agent_mind_bench.py` | **八阶段端到端海量盲测编排器**：`AgentMindBlindTest` / `MindDiagnostic` / `run_arena_agent_mind_bench`。只编排 C01/C02/C05/C06/C07/C04 生产引擎，五大铁律逐条断言，59 条断言全绿。 |
| `src/aios_core/simulation/mass_stress.py` | **海量吞吐极限压测执行器**：CI 档 + full 档同一代码，产出 P50/P95/P99 + tracemalloc 内存峰值 + 压缩比。 |
| `src/aios_core/tools/adaptive_temporal_compactor.py` | **TLP-C01 自适应时序压缩算子**（纯代码实现，0 依赖 0 模型调用） |
| `src/aios_core/tools/dual_lens_virtual_index.py` | **TLP-C02 双透镜虚拟索引投影器**（纯代码实现，0 依赖 0 模型调用） |

> 未改动 `src/aios_core/**` 任何既有生产文件（contracts/storage/query/…），
> 属于纯增量的测试平台 + 工具算子，与基底 1153 绿零冲突。

### 新增测试
| 文件 | 覆盖 |
|---|---|
| `tests/simulation/test_arena_agent_mind_bench.py` | 八阶段全执行 + 五铁律全绿 + 优劣 Agent 诊断器识别 |
| `tests/simulation/test_mass_stress.py` | 压测器诚实性（真实采样、压缩比账本、0 字节滞留、内存峰值） |
| `tests/tools/test_adaptive_temporal_compactor.py` | TLP-C01 平稳均值/突变独立/零模型/零导数/三通道 |
| `tests/tools/test_dual_lens_virtual_index.py` | TLP-C02 一次解析双透镜复用/惰性物化/只读/双时间一致 |
| `tests/tools/test_tool_proposals_arena.py` | ToolProposal 契约走查：两工具完整生命周期 + GAP-003 缺陷如实记录 |

### 报告交付物
| 文件 | 对应交付物 |
|---|---|
| `governance/agent_reports/arena_01a0a8c1/01_AIOS全流程海量盲测与极限压测报告.md` | 交付物一 |
| `governance/agent_reports/arena_01a0a8c1/02_全生命周期心智瓶颈与缺陷诊断书.md` | 交付物二 |
| `governance/agent_reports/arena_01a0a8c1/03_新机制发明与新工具提议.md` | 交付物三 |
| `governance/agent_reports/arena_01a0a8c1/04_完整代码与测试套件提交说明.md` | 交付物四（本文件） |

---

## 二、测试结果

```
$ pytest
1181 passed, 0 failed, 0 error  (41.1s)
```

- 既有 1153 项全绿（无回归）。
- 新增 28 项全绿：`test_arena_agent_mind_bench`(10) + `test_adaptive_temporal_compactor`(6)
  + `test_dual_lens_virtual_index`(4) + `test_mass_stress`(5) + `test_tool_proposals_arena`(3)。

## 三、复现命令（总工沙箱验收）

```bash
# 1) 全量单测
python -m pytest

# 2) 八阶段盲测（默认小档，秒级）
PYTHONPATH=src python -c "from aios_core.simulation.arena_agent_mind_bench import run_arena_agent_mind_bench; \
import json; print(json.dumps(run_arena_agent_mind_bench(db_path='/tmp/a.db'), ensure_ascii=False, indent=2)[:2000])"

# 3) 海量极限压测（full 档 1.2M 原始样本）
PYTHONPATH=src python -m aios_core.simulation.mass_stress --db /tmp/s.db --target 400000 --out /tmp/mass.json
```

## 四、审查红线自查

- ✅ 严禁自编 mock 自 assert：八阶段全部驱动生产引擎，断言命中引擎门禁或独立可复算证据（字节/SHA-256/行数/墙钟）。
- ✅ 无 `# TODO` / 占位符：新增代码零 TODO / 零 `pass` 占位。
- ✅ 只在本 session 分支提交，PR 目标 `arena/01a0a8c1-fantonghui → aios-2.0`。
- ✅ `.gitignore` 已排除 `.venv/`、`__pycache__`、临时 db/json（见 §五验证）。
