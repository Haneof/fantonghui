# 第二份：《AIOS认知工作台功能规格》

**版本**：V0.1  
**日期**：2026-09-14  
**依赖**：第一份规格的数据、时间、任务和版本约定。

---

## 1. 工作台定位

认知工作台是 AI 进入、观察、调查、修改和维护世界的统一工作环境。

工作台提供两种入口：
- **AI**：结构化操作接口。
- **开发者**：面板、按钮、时间缩放、关联展开与回放界面。

两者使用同一数据和命令规则。开发者修改必须带操作者标记；正式评分时，人工干预会被记录，不能混入自主成绩。

---

## 2. 界面布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 虚拟时间｜当前用户｜世界版本｜会话状态｜暂停/步进/回放          │
├──────────────────────────────────────────────────────────────┤
│ 紧急事项条：等级、依据、处理状态、剩余时限                      │
├───────────────┬─────────────────────────┬────────────────────┤
│ 本次唤醒       │ 当前世界                │ AI自身当前状态      │
│ 触发来源       │ 地点、人物、事件、变化   │ 角色、承诺、经验    │
│ 合并次数       │ 数据缺口、未确认内容     │ 目标、开放问题      │
├───────────────┼─────────────────────────┼────────────────────┤
│ 任务面板       │ 世界浏览区              │ 证据与主张区        │
│ 进行中/待办    │ 时间范围、粒度、维度     │ 来源、知识状态      │
│ 定时/截止      │ 多维对齐、搜索、下钻     │ 置信度、依赖        │
│ 观察/验证      │ 用户世界 ↔ AI世界        │ 冲突、修正历史      │
├───────────────┴─────────────────────────┴────────────────────┤
│ 操作轨迹｜世界修改｜行动收据｜结果｜待接续工作                 │
└──────────────────────────────────────────────────────────────┘
```

图中位置是第一版界面建议。AI 的操作能力不依赖具体屏幕排版。

---

## 3. 唤醒时的初始工作包

每次开始会话，工作台至少返回：

| 内容 | 具体要求 |
| :--- | :--- |
| 当前时间 | 虚拟当前时间、时区；真实运行耗时另记 |
| 可见世界版本 | 世界版本与信息截止时间 |
| 唤醒原因 | 类型、规则、命中资料、合并次数、时间窗口 |
| 紧急事项 | 未解决事项、等级、动作状态、处理期限 |
| 当前世界 | 最近已知地点、相关人物、主要事件、状态和变化 |
| 任务摘要 | 正在执行、已到期、临近截止、等待结果、阻塞 |
| 承诺 | 未完成承诺及其任务引用 |
| 未确认认知 | 冲突、新证据、需要复核的主张 |
| AI自身状态 | 身份、当前目标、关系理解、最近策略和经验 |
| 数据质量 | 数据延迟、缺失、低质量资料、过期总结 |
| 操作范围 | 可用工具、权限、预算和接续能力 |

面板必须有引用入口，不能只给一段无法展开的自然语言摘要。  
“当前地点”必须显示该信息何时获得、是否已经过时。没有资料时显示未知，不能根据惯常作息补成事实。  
初始工作包只装载相关摘要和索引，其他资料由 AI 按需展开。被省略内容必须显示数量和查询入口。

---

## 4. 公共操作协议

每次操作请求包含：  
`operation_id`、`session_id`、`operation_name`、`arguments`、`expected_world_revision`。

读取操作可增加：  
`knowledge_cutoff`、`cursor`、`limit`。

修改操作必须增加：  
`reason`、`idempotency_key`。

每次响应包含：  
`status`、`world_revision`、`data`、`source_refs`、`coverage`、`warnings`、`next_cursor`。

统一错误包括：
- `INVALID_ARGUMENT`：参数不合法。
- `NOT_FOUND`：对象不存在。
- `VERSION_CONFLICT`：对象已被其他操作修改。
- `INCOMPLETE_DATA`：可见资料不足。
- `STALE_INDEX`：查询索引落后。
- `BUDGET_EXHAUSTED`：预算用尽，需要检查点或调度续办。
- `PERMISSION_DENIED`：超出运行权限。
- `DEPENDENCY_INVALID`：依赖已失效。
- `OUTCOME_UNKNOWN`：执行结果尚不能确认。

空结果必须区分“确实没有记录”和“查询未完成/来源缺失”。

---

## 5. AI操作与对应按钮

### 5.1 当前工作环境

| 人类按钮/操作 | AI接口 | 主要输入与结果 |
| :--- | :--- | :--- |
| 打开当前面板 | `workspace.open` | 返回初始工作包与快照 |
| 查看变化 | `workspace.changes` | 从某世界版本起，返回新增、修正、失效 |
| 打开AI状态 | `self.inspect` | 身份、目标、承诺、策略、开放问题 |
| 保存检查点 | `session.checkpoint` | 已完成工作、提交、下一步、未决问题 |
| 结束/接续 | `session.finish` | 完成、等待、继续、失败及对应原因 |

界面操作“暂停运行”只控制开发环境；AI 等待未来条件使用任务和观察机制。

### 5.2 时间与维度

| 人类按钮/操作 | AI接口 | 主要输入与结果 |
| :--- | :--- | :--- |
| 选择时间范围 | `world.view` | 主体、起止时间、粒度、维度、可见性截止 |
| 放大/缩小 | `world.zoom` | 既有视图、目标粒度、锚点 |
| 前移/后移 | `world.shift` | 视图、位移；禁止越过可见信息边界 |
| 添加/隐藏维度 | `world.select_dimensions` | 改变观察集合，不删除世界数据 |
| 同时对齐 | `world.align` | 多维度、多主体、同一时间范围 |
| 比较两个时期 | `world.compare` | 两个区间、维度、指标与数据质量 |
| 找变化段 | `world.detect_changes` | 数值变化、状态切换、缺失等机械检测 |
| 查看维度定义 | `dimension.inspect` | 含义、来源、生命周期、维护方式 |

必须支持秒、分、时、日、周、月、季度、半年、年及多年范围。  
不同数据使用不同表现：
- 数值：曲线与统计。
- 状态：状态区间。
- 事件：锚点与事件展开。
- 人物：实体与身份历史。
- 关系：关系网络及有效期。
- 文本：原话节点。
- 高阶认知：主张、证据结构及变化历史。

比较工具可以算出差异与共变，不能自动把相关性写成因果。

### 5.3 搜索与下钻

| 人类按钮/操作 | AI接口 | 主要输入与结果 |
| :--- | :--- | :--- |
| 搜索世界 | `world.search` | 关键词、概念、实体、关系、时间、主体、排序 |
| 展开事件 | `event.expand` | 事件ID；参与对象、范围、主张、证据 |
| 展开人物/物品 | `entity.expand` | 身份、别名、关系、相关事件 |
| 展开关联 | `world.follow_links` | 对象、关联类型、深度、时间范围 |
| 查看原始证据 | `evidence.read` | 对象ID与版本，返回内容和来源 |
| 追问为什么 | `evidence.trace` | 从主张向下追溯支持、反对及缺失证据 |
| 查影响范围 | `dependency.inspect` | 某对象改变可能影响的认知、总结和任务 |
| 展开总结 | `summary.expand` | 年→月→日→节点，显示覆盖与缺口 |

搜索“妈妈”时先解析候选实体及其身份主张，不能将所有包含“妈妈”的文字自动视作同一个人物。  
复杂查询必须支持分阶段扩大召回。例如先按人物与关系找事件，再补充关键词、时间和原话。

### 5.4 修改世界

| 人类按钮/操作 | AI接口 | 主要输入与结果 |
| :--- | :--- | :--- |
| 新建主张 | `claim.create` | 内容、知识状态、置信度、证据、未知项 |
| 修正主张 | `claim.revise` | 旧版本、新内容、修正原因及证据 |
| 建立事件 | `event.create` | 事件解释、时间范围、实体、支持资料 |
| 更新身份 | `entity.resolve` | 稳定实体ID、候选身份、依据 |
| 建立/修正关联 | `relation.upsert` | 两端对象、类型、有效期、主张 |
| 新建总结 | `summary.create` | 范围、维度、来源、主张、覆盖情况 |
| 提议维度 | `dimension.propose` | 价值、来源、更新方法、重复性、维护成本 |
| 调整维度 | `dimension.transition` | 生命周期转换、合并/拆分映射与依据 |

所有修改返回新的世界版本和受影响对象清单。AI 无权通过修改历史直接抹掉旧判断。

### 5.5 任务、触发、行动与经验

| 人类按钮/操作 | AI接口 | 主要输入与结果 |
| :--- | :--- | :--- |
| 创建任务 | `task.create` | 类型、目标、依据、时限、完成/取消条件 |
| 修改/暂停/取消 | `task.update` | 当前版本、修改内容与原因 |
| 查看任务 | `task.query` | 状态、类型、人物、App、时间条件 |
| 登记观察 | `watch.register` | 来源、条件、窗口、期限、复查规则 |
| 调整触发 | `trigger.update` | 可调参数、有效期、依据、回滚条件 |
| 发起帮助 | `action.propose` | 内容、对象、通道、任务关联、预期结果 |
| 查询收据 | `action.status` | 执行ID、执行状态、结果状态 |
| 登记经验 | `experience.record` | 方法、适用范围、成本、遗漏、验证结果 |
| 查找经验 | `experience.search` | 当前问题类型、条件、既有失败 |
| 提议新工具 | `tool.propose` | 重复需求、现有不足、预期接口与收益 |

接口不是强制思考路线。AI 可自行选择调用顺序、扩大范围或结束调查。

---

## 6. 六类触发的工作方式

| 触发 | 机械输入 | 唤醒内容 |
| :--- | :--- | :--- |
| 数值/状态变化 | 幅度、趋势、状态切换、持续时间 | 变化片段与参考基线 |
| 关键词/实体 | 已登记别名、实体引用、匹配位置 | 原句、实体候选、上下文 |
| 长时间无变化 | 指定来源/维度在窗口内无更新 | 窗口、来源健康状态、已知缺口 |
| 定时/截止 | 到期实例、临近截止、周期规则 | 任务、剩余时间、依赖状态 |
| 用户主动 | 用户输入及交互信号 | 请求与当前世界 |
| 人身安全 | 预定义安全信号或模拟紧急输入 | 信号、级别、应急动作状态 |

“无变化”必须先区别：
1. 有持续采样但状态稳定。
2. 来源停止上报。
3. 来源本来就不是持续采样。

三种情况都可能值得检查，但不能直接判断孤独、休息或遇险。  
重复关键词需要去重与冷却。冷却期间积累计数与证据；强度升级、显著新证据或独立安全信号应能重新唤醒。

---

## 7. 任务中心的十类任务

### 7.1 共同任务结构
在宪法要求上补充以下工程字段：  
`task_id`、`task_type`、`owner_subject_id`、`goal`、`reason_refs`、`priority`、`created_at`、`next_wake_at`、`deadline`、`recurrence`、`timezone`、`dependencies`、`state`、`next_step`、`completion_condition`、`cancel_condition`、`related_entities`、`app_id`、`attempts`、`execution_refs`、`outcome_refs`。

完成条件必须能被证据检查。例如：
- “通知已送达”只完成通知任务。
- “用户已掌握某知识点”需要测验结果，不能用消息送达代替。
- “等待用户反馈”处于等待状态，不能假设用户接受。

### 7.2 各类型规则

| 类型 | 激活方式 | 关键规则 |
| :--- | :--- | :--- |
| 即时任务 | 创建后进入就绪队列 | 立即处理仍需去重与执行收据 |
| 定时任务 | 一次绝对时间或相对延迟 | 到期唤醒AI，再判断当前处理方式 |
| 截止任务 | 提前检查与截止时间 | 超时标记，不能悄悄顺延 |
| 待办任务 | 有目标但无固定执行时间 | 必须进入定期任务盘点或相关情境唤醒 |
| 周期任务 | 日历规则生成任务实例 | 每次实例独立完成；父任务持续存在 |
| 跟进任务 | 原行动之后的时间或条件 | 关联原行动和预期结果 |
| 观察任务 | 注册条件与有限观察期 | 命中或到期都产生可处理结果 |
| 验证任务 | 新证据或复查时间 | 可以得出“仍不确定”，不能被迫确认 |
| 后台维护任务 | 依赖失效、总结到期、登记维护需求 | 可延后但不能无期限饿死 |
| App任务 | 场景目标 | 使用相同状态与时间机制，不私建定时器 |

### 7.3 任务状态机

```text
DRAFT → READY → RUNNING
                 ├→ WAITING_TIME
                 ├→ WAITING_EVIDENCE
                 ├→ WAITING_USER
                 ├→ WAITING_RESULT
                 ├→ BLOCKED
                 ├→ COMPLETED
                 ├→ FAILED
                 ├→ EXPIRED
                 └→ CANCELLED

等待条件满足 → READY
RUNNING 中断 → 恢复核对 → READY / 等待状态 / FAILED
```

周期任务的每次执行使用独立 `occurrence_id`。取消父任务会停止未来实例，并明确处理已经就绪或正在执行的实例。  
任务重试必须设置次数、退避、截止约束和失败去向；超过上限进入可见阻塞或失败状态。

### 7.4 时间细节
第一版必须明确：
- “明天”按任务所属时区解释。
- “一年后”与“360天后”是不同规则。
- 月末不存在对应日期时，采用任务中登记的处理策略。
- 停机后到期任务按“补执行、跳过、合并或过期”策略恢复，逐项留痕。
- 修改时间规则生成新版本，不改变已完成实例的历史。
- 待办没有截止时间，但必须有下一次盘点时间或明确的阻塞依赖。

---

## 8. 观察任务与验证任务

观察任务至少包含：  
`observe_scope`、`start_at`、`end_at`、`condition`、`check_schedule`、`cooldown`、`on_match`、`on_timeout`。

条件分两层：
1. **可以机械计算**：例如七天练习次数低于登记值、收到相关实体资料。
2. **需要语义判断**：例如是否出现持续学习挫败。由时间/数据变化唤醒 AI 后复核。

禁止在本地观察器中把语义判断硬编码成结论。  
观察期结束必须记录：  
`MATCHED` / `NOT_MATCHED_WITH_SUFFICIENT_DATA` / `INSUFFICIENT_DATA`。  
“没有发现”与“缺资料”分开。

验证任务绑定具体主张及其证据缺口。出现新证据时复核，并按需要延长观察、修正认知或结束验证。

---

## 9. 紧急事项面板

紧急事项首先是一条待处理警报，尚不等于 AI 已确认的人生事件。

至少显示：
- 来源信号与时间。
- 等级及处理时限。
- 是否已接收、核实、升级。
- 正在执行的模拟应急动作。
- 是否等待用户响应。
- 最近状态与负责会话。

建议状态为：  
`NEW` → `ACKNOWLEDGED` → `CHECKING` → `HANDLING` / `ESCALATED` → `RESOLVED` / `FALSE_ALARM`。

“已接收”不能清除未解决警报。结束必须有处理结果或误报依据。  
安全测试动作走独立高优先通道；普通会话保存检查点后让出调度。模型不可用时仍执行已定义的机械模拟安全流程。

---

## 10. 十三步认知循环如何落实

工作台按宪法第三十四条记录：
1. 唤醒来源。
2. 紧急程度检查。
3. 当前世界检查。
4. AI自身状态与承诺检查。
5. 观察计划。
6. 工具操作。
7. 认知产出。
8. 世界更新。
9. 帮助决策。
10. 后续任务。
11. 行动与等待。
12. 结果回写。
13. AI自身更新。

执行约定：
- 工具调查可以反复、分支、回退。
- 一个会话不必等到几天后的结果才能结束。
- 等待未来结果时，必须创建任务并保存检查点。
- 第12、13步可由后续结果会话继续，但必须能追溯原行动。
- 每一阶段记录“完成、无需处理、等待、失败”及简短依据。
- 不要求生成或保存模型的私密思维链；保存可观察的工具调用、引用、判断摘要、决定和结果即可支持审计。

---

## 11. 主动帮助的决策记录

每个帮助决定至少记录：
- 发现了什么机会或问题。
- 与用户哪个目标、状态或承诺有关。
- 证据是否足够。
- 为什么现在介入或暂缓。
- 选择的方式、时机与强度。
- 预期改善。
- 如何观察结果。

允许结果：  
`ACT_NOW` / `ASK` / `SUGGEST` / `DEFER` / `WATCH` / `SILENCE`。

沉默不算失败，也不自动算成功。评分由当时情境是否值得介入决定。  
用户拒绝某次帮助，只更新相应情境和策略，不自动推导为“以后永远不要帮助”。

---

## 12. AI操作经验与维度注册

### 操作经验
记录问题类型、工具路径、证据覆盖、调用成本、结果、遗漏、适用条件和反例。  
只有经历后续验证，才从候选经验变成优先建议。经验能够过期、被否定和恢复。  
例如“先查人物关系再查关键词”是可检验的检索策略，不能被固定成每次必须执行的流程。

### 维度注册
AI 提交：
- 定义与主体。
- 已有维度为何不足。
- 支撑资料和更新规则。
- 预期帮助价值。
- 与已有维度的重叠。
- 维护成本和失效条件。

系统先做结构、引用、权限和重复名称检查。需要语义比较的审核由大模型审查流程承担，不能由本地规则假装判断长期价值。  
建议采用“候选试用 → 观察收益 → 正式活跃”的机制。容量或维护预算耗尽时保留候选并说明原因，不能静默丢弃。

---

## 13. 贯穿验收场景

给虚拟用户连续提供英语练习、题目难度、睡眠、工作安排和交流资料，不让用户问“为什么退步”。

期望过程：
1. AI 被变化或巡检唤醒。
2. 查看练习趋势，并核对是否只是难度提高。
3. 自主寻找其他维度证据。
4. 形成适当置信度的解释。
5. 判断是否需要帮助及当前时机。
6. 提出或执行允许的教育调整。
7. 创建跟进和学习效果验证任务。
8. 后续根据表现修正判断与策略。

同一测试还必须提供“成绩下降但能力增长”的对照情境，检验 AI 是否会误报退步。

---

## 14. 工作台验收编号

- **W01**：面板字段完整，未知与过时信息可辨。
- **W02**：AI操作与界面按钮走同一接口。
- **W03**：缩放、筛选、搜索、下钻保持主体和时间范围。
- **W04**：区间比较返回覆盖率，不将缺失视为零。
- **W05**：六类触发可独立验证，重复唤醒可合并。
- **W06**：十类任务都有完整状态与恢复行为。
- **W07**：紧急事项可抢占，未解决警报不被确认按钮清除。
- **W08**：结果超时保持未知，重试不重复行动。
- **W09**：承诺必须关联任务；分析结束后未来工作仍可继续。
- **W10**：AI能选择沉默，并留下检查依据。
- **W11**：新维度与操作经验具有验证和退出机制。
- **W12**：模型中断后可从操作和提交记录继续。
