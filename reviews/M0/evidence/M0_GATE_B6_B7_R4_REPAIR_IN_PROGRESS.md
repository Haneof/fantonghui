# M0 Gate B6/B7/B5/R4 repair — CI trigger

Semantic repair commit under test: `f38fdd2aa64e31b92c5353206a8aef62c9322087`.

This evidence marker intentionally triggers the normal branch CI because the one-shot workflow's `GITHUB_TOKEN` push does not recursively start workflows. It is not a FINAL PASS record.
