# M0-009 FINAL PASS

日期：2026-09-14

任务：EvidenceSet一等证据集合

冻结commit：cda888f371fed812cda1b5e08d8a27db5e0c240a

分支：arena/01a09bc6-fantonghui

GitHub Actions：Python 3.12.14 234 passed Reference 15 passed SUCCESS

## 冻结内容

- EvidenceSet不是动态query字符串，是一等可复核可冻结可重建对象
- explicit member模式：member_refs [A@1,B@1] selector None合法 commit round-trip
- selector模式：selector存在 member []合法，selector+member非空表示已材料化
- evidence refs必须pinned：member_refs/support_refs/counter_refs/context_refs只允许ObjectRef revision=N，禁止revision None，历史证据不能随target latest变化，E04/E05/E19
- selector dimension refs必须pinned：dimension_refs每个revision非None，否则ValidationError，E06
- fixed TemporalExtent：selector time_range固定绝对时间 2026-09-01->2026-09-14，一周后读取旧EvidenceSet仍原区间，不自动变成now-14d，禁止relative_time/last_n_days/dynamic_window，extra forbid，E09/E10
- knowledge cutoff冻结：knowledge_window.knowledge_cutoff <= learned_at per as_utc，系统不能在10:00创建EvidenceSet却声称快照允许知道11:00资料，cutoff 10:00合法 09:00合法 11:00非法，E13
- KnowledgeWindow world_revision round-trip：world_revision 7保存仍7，不自动改当前，M1-006以后负责真实赋值，E14
- support/counter/context分离：support [S@1] counter [C@1] context [X@1] round-trip严格分离不污染不合并，E11
- coverage/missingness：expected 10 observed 7 ratio 0.7 missing ["09:30-09:45 心率缺失","没有比赛名称"] model dump和SQLite round-trip保持，E12，ratio bounds 0.0/0.5/1.0合法 -0.01/1.01非法，E17
- pinned history：target rev1 old EvidenceSet member @1 target rev2 new member仍rev1 old，E15
- revision-based stale/rebuild capability：rev1 [A] stale False, rev2 [A] stale True, rev3 [A,B] stale False新knowledge_window，rev1不变 rev2可读 rev3可读members+1，证明schema支持stale/rebuild通过新revision，不实现自动mark，E16
- object_type固定：EVENT => ValidationError，E18
- exact schema：E01 purpose str, knowledge_window KnowledgeWindow, member/support/counter/context list[ObjectRef], selector EvidenceSelector|None exact set {EvidenceSelector,None}, selection_method str, aggregation_method str|None exact set, coverage EvidenceCoverage, stale bool, selector time_range TemporalExtent dimension_refs list[ObjectRef] filters dict[str,Any] exact, coverage expected int|None set等，E19 list[ObjectRef] exact

## durable boundary generic Pydantic revalidation

- SQLiteWorldStore.commit在最终持久化前generic重新验证完整WorldObject对象图
- 顺序：idempotency replay → expected world revision → persistence revalidation → revision/type → reference validation → write
- 不存在EvidenceSet hardcode
- 实现：model_dump python round_trip True + model_validate snapshot, ValidationError => StoreError INVALID_ARGUMENT context object_id, object_type, revision, reason persistence_revalidation_failed from exc, 禁止泄露payload
- 原子性：任何INSERT之前，失败rollback，world revision不增加，无成功idempotency record
- 新增E20-E23：member_refs原地mutation, selector.dimension_refs嵌套mutation, coverage mutation 1.5, time_range mutation "now-14d"，均必须INVALID_ARGUMENT，world revision不增加，EvidenceSet不存在，移除revalidation时旧实现会错误提交
- E23有1条故意非法mutation产生的Pydantic serializer warning，属于adversarial fixture预期，不是生产正常路径warning

## 生产diff

- models.py：新增pinned checks和cutoff check，符合允许范围
- sqlite_store.py：新增generic revalidation，符合R1允许范围
- 其他src：NO CHANGE

## 测试

Python 3.12.14 234 passed (211+23), Reference 15 passed

E23 1条故意非法mutation warning预期

## 最终

M0-009 FINAL PASS

允许：M0-010
